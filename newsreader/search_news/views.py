from django.shortcuts import render

'''
Here we define what GELPI (aka L'HOME)
calls 'controllers'. Each user input will go
to a specific 'function' (aka, view). Then, the
view will 'answer' the user with an HttpResponse,
calling a template
'''
